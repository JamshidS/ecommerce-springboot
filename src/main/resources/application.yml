server:
  port: 8081


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/my_ecommerce
    driverClassName: org.postgresql.Driver
    username: postgres
    password: 12345git 

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        hbm2ddl:
          auto: update
